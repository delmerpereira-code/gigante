# Deploy estático — publica web/ direto, sem build (web/config.js já está no repo).
[build]
  publish = "web"
  command = ""

[[headers]]
  for = "/sw.js"
  [headers.values]
    Cache-Control = "no-cache"
