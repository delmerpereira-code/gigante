# clasp — credenciais e config local
.clasp.json
.clasprc.json
apps-script/.clasp.json

# Supabase — a chave ANON é pública por design (RLS protege os dados),
# então web/config.js VAI para o repositório privado. A service_role NUNCA.
supabase/.env

# node
node_modules/

# sistema
.DS_Store
Thumbs.db
*.log
